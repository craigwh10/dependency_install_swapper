{
  "name": "javascript",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "parcel popup.html",
    "build": "rm -rf output && parcel build src/content/index.ts popup.html --public-url=./ --no-source-maps && cp manifest.json output/manifest.json && cp -r assets output/assets",
    "test:unit": "jest --config=./jest.config.js"
  },
  "targets": {
    "default": {
      "distDir": "./output",
      "includeNodeModules": true
    }
  },
  "alias": {
    "@js/*": "./$1"
  }
}
